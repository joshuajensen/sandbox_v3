define(['jquery','backbone'], 
    function($, Backbone){
//Take care of global app tasks...

	      window.App = {};
					
			App.vent = _.extend({}, Backbone.Events);

			

			App.page = {};
return App;
});
